<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lato:300,300i,700">
  <title>Forgot Password</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f9f9f9;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .container {
      background-color: #fff;
      padding: 3rem;
      border-radius: 10px;
      box-shadow: 0 1rem 2rem rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 500px;
      text-align: center;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 2rem;
      color: #333;
    }

    .form__input {
      display: block;
      font-family: inherit;
      font-size: 1rem;
      color: #333;
      padding: 1.1rem 8rem;
      padding-left: 5%;
      border: none;
      width: 100%;
      background-color: #f2f2f2;
      border-top: 3px solid transparent;
      border-bottom: 3px solid transparent;
      transition: all 0.3s;
      border-radius: 4px;
      box-sizing: border-box;
      outline: none;
      margin-bottom: 2rem;
    }

    .form__input:focus {
      outline: none;
      border-bottom: 3px solid #55c57a;
    }

    .form__input:focus:invalid {
      border-bottom: 3px solid #ff7730;
    }

    .form__input::-webkit-input-placeholder {
      color: #bbb;
    }

    .alert {
      display: flex;
      align-items: center;
      position: fixed;
      top: 10%;
      left: 50%;
      -webkit-transform: translateX(-50%);
      transform: translateX(-50%);
      z-index: 9999;
      color: black !important;
      background-color: white !important;
      font-size: 1.2rem;
      font-weight: 600 !important;
      /* text-align: center !important; */
      font-weight: 500;
      padding: 1.6rem 15rem;
      width: max-content;
      border-radius: 16px !important;
      padding: 1rem 3rem !important;
      -webkit-box-shadow: 0 2rem 4rem rgba(0, 0, 0, 0.25);
      box-shadow: 0 2rem 4rem rgba(0, 0, 0, 0.25);
    }

    .alert--success {
      color: #20bf6b !important;
    }

    .alert--error {
      color: hsl(1, 100%, 49%) !important;
    }

    .btn {
      font-size: 1rem;
      padding: 1rem 1rem;
      border-radius: 10rem;
      text-transform: uppercase;
      display: inline-block;
      text-decoration: none;
      position: relative;
      transition: all 0.4s;
      font-weight: 600;
      backface-visibility: hidden;
      border: none;
      cursor: pointer;
    }

    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 1rem 2rem rgba(0, 0, 0, 0.15);
    }

    .btn:active {
      transform: translateY(-1px);
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
    }

    .btn:focus {
      outline: none;
      background-color: #2e864b;
    }

    .btn--green {
      background-color: #55c57a;
      color: #fff;
      margin-top: 2rem;
      display: inline-block;
    }

    .btn--green::after {
      background-color: #55c57a;
    }

    /* Center the form and button */
    form {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    label {
      display: block;
      text-align: left;
      width: 100%;
      margin-bottom: 0.5rem;
      font-size: 1.4rem;
      color: #555;
    }

    .ma-bt-lg {
      margin-bottom: 3.5rem !important;
    }

    .heading-secondary {
      font-size: 2.25rem;
      text-transform: uppercase;
      font-weight: 700;
      background-image: -webkit-gradient(linear,
          left top,
          right top,
          from(#7dd56f),
          to(#28b487));
      background-image: linear-gradient(to right, #7dd56f, #28b487);
      -webkit-background-clip: text;
      color: transparent;
      letter-spacing: 0.1rem;
      line-height: 1.3;
      display: inline-block;
    }
  </style>
</head>

<body>
  <div class="container">
    <h2 class="heading-secondary ma-bt-lg">Enter Your Email</h1>
      <form class="form form--forgotPassword" name="forgotPasswordForm">
        <div class="form__group ma-bt-md">
          <label class="form__label" for="email">Email:</label>
          <input id="email" class="form__input" type="email" name="email" placeholder="you@example.com" required
            autocomplete="username">
        </div>
        <button class="btn btn--green btn-forgot" type="submit">Forgot Password</button>
  </div>
  </form>
  </div>
  <!-- <script type="module" src="/js/forgotPassword.js"></script> -->
  <script src="https://js.stripe.com/v3/"></script>
  <script src="/js/bundle.js"></script>
</body>

</html>