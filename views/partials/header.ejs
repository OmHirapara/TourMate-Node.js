<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="shortcut icon" type="image/png" href="/img/favicon.png">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lato:300,300i,700">
  <script defer src="/js/validator-signup.js"></script>
  <title>TourMate | <%= title %>
  </title>
</head>

<body>
  <header class="header">
    <nav class="nav nav--tours">
      <a class="nav__el" href="/">All tours</a>
    </nav>
    <div class="header__logo">
      <img src="/img/logo-white.png" alt="TourMate logo">
    </div>
    <nav class="nav nav--user">
      <% if (typeof user !=='undefined' && user) { %>
      <a class="nav__el nav__el--logout">Log out</a>
      <a class="nav__el" href="/me">
        <img class="nav__user-img" src="/img/users/<%= user.photo %>" alt="Photo of <%= user.name %>">
        <span>
          <%= user.name.split(' ')[0] %></span>
      </a>
      <% } else { %>
      <a class="nav__el" href="/login">Log in</a>
      <a class="nav__el nav__el--cta" href="/signup">Sign up</a>
      <% } %>
    </nav>
  </header>